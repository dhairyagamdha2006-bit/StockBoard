# Screenshots

Drop product screenshots/GIFs here and reference them from the root `README.md`:

- `dashboard.png` — main dashboard (hero value, metrics, chart, holdings)
- `holdings.png` — holdings table with search/sort/filter + CSV export
- `analytics.png` — sector breakdown, allocation treemap, gainers/losers

Tip: load demo data first (dashboard → **Load Demo Data**) so the screenshots
show a populated, realistic portfolio without exposing real account data.
